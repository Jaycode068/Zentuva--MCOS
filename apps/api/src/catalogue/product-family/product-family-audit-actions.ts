/**
 * Audit action strings for `ProductFamily` (Sprint 4.7, docs/domains/catalogue.md). Same
 * `<entity>.<event>` naming convention as `PRODUCT_AUDIT_ACTIONS`.
 */
export const PRODUCT_FAMILY_AUDIT_ACTIONS = {
  CREATED: 'product-family.created',
  UPDATED: 'product-family.updated',
} as const;
