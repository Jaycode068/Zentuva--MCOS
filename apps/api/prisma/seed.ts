/**
 * Placeholder seed script. No business domains exist yet, so there is no data to seed.
 * Domain modules should add their own seed logic here as they are built (e.g. reference
 * data, a default tenant for local dev), per docs/domains/<domain>.md.
 */
async function main(): Promise<void> {
  console.log('No seed data yet — this is a placeholder for future domain seeders.');
}

main();
