{
  "name": "@zentuva/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types used across Zentuva apps and packages",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@zentuva/config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.6.3"
  }
}
