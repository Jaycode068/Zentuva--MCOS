{
  "name": "@zentuva/validation",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod validation schemas used across Zentuva apps and packages",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@zentuva/config": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.6.3"
  }
}
